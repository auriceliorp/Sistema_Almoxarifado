# Flask: framework principal usado para desenvolver a aplicação web
Flask==2.2.3

# Flask-Login: gerencia autenticação de usuários e sessões
Flask-Login==0.6.2

# Flask-SQLAlchemy: integração do SQLAlchemy com Flask para ORM
Flask-SQLAlchemy==3.0.3

# SQLAlchemy: ORM usado para comunicação com o banco de dados (Flask-SQLAlchemy depende dele)
SQLAlchemy==2.0.7

# Werkzeug: biblioteca WSGI usada internamente pelo Flask para lidar com requisições e respostas
Werkzeug==2.2.3

# email-validator: validação de e-mails (útil para formulários de cadastro, por exemplo)
email-validator==1.3.1

# python-dotenv: permite carregar variáveis de ambiente de um arquivo `.env` (como senhas, configs de banco etc.)
python-dotenv==1.0.0

# gunicorn: servidor WSGI para execução em produção (necessário no Render)
gunicorn==20.1.0

# psycopg2-binary: driver do PostgreSQL para Python, usado para conectar ao banco de dados
psycopg2-binary==2.9.5

# pandas: análise e manipulação de dados, usado em relatórios e importação/exportação de planilhas
pandas

# openpyxl: permite leitura e escrita de arquivos Excel (.xlsx), usado com pandas
openpyxl

# fpdf: biblioteca para geração de arquivos PDF (por exemplo, relatórios)
fpdf==1.7.2

# flask-migrate: integra Flask com Alembic, permitindo migrações do banco de dados (upgrade da estrutura)
flask-migrate==4.0.5
