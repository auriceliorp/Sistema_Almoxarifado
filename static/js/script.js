// JavaScript personalizado para o Sistema de Almoxarifado

document.addEventListener("DOMContentLoaded", function() {
    // Exemplo: Adicionar confirmação extra em botões perigosos (Bootstrap já faz isso com onsubmit)
    // const deleteButtons = document.querySelectorAll(".btn-outline-danger");
    // deleteButtons.forEach(button => {
    //     button.addEventListener("click", function(event) {
    //         if (!confirm("Tem certeza que deseja executar esta ação?")) {
    //             event.preventDefault();
    //         }
    //     });
    // });

    // Outras interatividades podem ser adicionadas aqui
    console.log("Sistema de Almoxarifado JS carregado.");
});

